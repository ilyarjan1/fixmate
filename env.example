# Database (Prisma)
# Local SQLite
DATABASE_URL="file:./dev.db"

# Production Postgres (Example for Supabase/Vercel)
# DATABASE_URL="postgresql://user:password@host:5432/mydb"

# NextAuth Secret (Generate with `npx auth secret` or `openssl rand -base64 32`)
AUTH_SECRET="your-secret-key-here-change-this"

# Admin Credentials (Hardcoded in auth.ts for demo)
# Default: admin@fixmate.com / admin123

# Telegram Notifications
TELEGRAM_BOT_TOKEN="your-bot-token-from-botfather"
TELEGRAM_CHAT_ID="your-chat-id"
